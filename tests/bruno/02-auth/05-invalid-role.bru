meta {
  name: Invalid Role (400)
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/auth/token
  body: json
  auth: none
}

body:json {
  {
    "name": "nobody",
    "role": "superuser"
  }
}

tests {
  test("status 400", function() {
    expect(res.status).to.equal(400);
  });
}
