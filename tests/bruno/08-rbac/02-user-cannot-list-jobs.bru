meta {
  name: RBAC — User cannot list drone jobs (403)
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/drone/jobs
  body: none
  auth: bearer
}

auth:bearer {
  token: {{userToken}}
}

tests {
  test("status 403", function() {
    expect(res.status).to.equal(403);
  });
}
