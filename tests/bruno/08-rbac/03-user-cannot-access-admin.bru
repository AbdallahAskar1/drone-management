meta {
  name: RBAC — User cannot access admin endpoints (403)
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/admin/orders
  body: none
  auth: bearer
}

auth:bearer {
  token: {{userToken}}
}

tests {
  test("status 403", function() {
    expect(res.status).to.equal(403);
  });
}
