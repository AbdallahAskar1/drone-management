meta {
  name: Submit Order
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/orders
  body: json
  auth: bearer
}

auth:bearer {
  token: {{userToken}}
}

body:json {
  {
    "origin": { "lat": 30.0, "lng": 31.0 },
    "destination": { "lat": 30.5, "lng": 31.5 }
  }
}

script:post-response {
  if (res.status === 201) {
    bru.setVar("orderId", res.body.id);
  }
}

tests {
  test("status 201", function() {
    expect(res.status).to.equal(201);
  });
  test("status is pending", function() {
    expect(res.body.status).to.equal("pending");
  });
  test("orderId saved", function() {
    expect(bru.getVar("orderId")).to.not.be.empty;
  });
}
