meta {
  name: Submit Order for Handoff Test
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/orders
  body: json
  auth: bearer
}

auth:bearer {
  token: {{userToken}}
}

body:json {
  {
    "origin": { "lat": 29.0, "lng": 30.0 },
    "destination": { "lat": 29.9, "lng": 30.9 }
  }
}

script:post-response {
  if (res.status === 201) {
    bru.setVar("orderId", res.body.id);
  }
}

tests {
  test("status 201", function() {
    expect(res.status).to.equal(201);
  });
}
