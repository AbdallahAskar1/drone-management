meta {
  name: Drone1 — Reserve Job
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/drone/jobs/{{jobId}}/reserve
  body: none
  auth: bearer
}

auth:bearer {
  token: {{droneToken}}
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
}
